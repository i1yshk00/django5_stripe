"""Совместимый модуль-обертка для импорта базовых настроек.

Файл сохранен, чтобы старые импорты `django5_stripe.settings.settings`
не ломались после перехода на пакетную структуру настроек.
"""

from .base import *
