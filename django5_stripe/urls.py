"""Корневой URLConf проекта.

Здесь остаются только маршруты верхнего уровня:
- административная панель Django;
- подключение URL-ов приложения `api`, в котором будет жить бизнес-логика
  тестового задания.
"""

from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    # Административная панель нужна и для бонусного пункта ТЗ, и для
    # дальнейшего наполнения демо-данными перед деплоем.
    path('admin/', admin.site.urls),
    # Все пользовательские маршруты приложения подключаются из api.urls.
    path('', include('api.urls')),
]
